var seedData = require("./seeds.json");
module.exports = {
  compliments: seedData
  // this compliments is looking at seeds.json and the compliments property is being accessed in index.js at compliments: db.compliments
};
