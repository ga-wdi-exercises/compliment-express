<div>
    {{#if name}}
    <h3>{{name}}'s emergency compliment</h3>
    {{else}}
    <h3>emergency compliment</h3>
    {{/if}}
</div>
<h1>{{compliment}}</h1>
<div class="buttons">
    <a href="#">Thanks!</a>
    <a href="#">meh.</a>
</div>
