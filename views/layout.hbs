<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>THE BEST COMPLIMENTS EVER!</title>
  </head>
  <style>
    header {
      font-size: 50px;
    }
    p {
      position: relative;
      top: 50%;
      transform: translateY(-50%);
      -webkit-transform: translateY(-50%);
    }
    .app_body {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .compliment_box {
      font-family: sans-serif;
      font-size: 20px;
      width: 800px;
      height: 450px;
      text-align: center;
    }
    .form_box {
      font-family: sans-serif;
      font-size: 20px;
      width: 800px;
      height: 450;
      text-align: center;
    }
    button {
      height: 40px;
      width: 100px;
      font-family: sans-serif;
      font-size: 20px;
    }
    input {
      font-family: sans-serif;
      font-size: 22px;
      background-color: white;
      height: 50px;
      width: 150px;
    }

    #button1 {
      align-self: flex-end;
      margin: auto;
      position: relative;
      margin-top: 8em;
    }
    #button2 {
      align-self: flex-end;
      margin: auto;
      position: center;
      margin-top: 4em;
    }
  <body>

  </body>
</html>
